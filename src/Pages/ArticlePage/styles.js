import styled from "styled-components";

export const Wrapper = styled.div`
    padding: 50px 0 0 50px;
`

export const paragraphWrapper = styled.h2.attrs(()=> ({
    className: 'headline-h2',
}))`
    
`